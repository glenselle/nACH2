{
  "name": "@industriousoffice/nacha",
  "description": "nACH is a highly customizable Node.js module exposing a high & low-level API for generating ACH files for use within the ACH network",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/IndustriousOfficeLLC/nacha.git"
  },
  "files": [
    "lib/**/*.js",
    "index.js"
  ],
  "engines": {
    "node": ">= 0.10.0"
  },
  "scripts": {
    "build": "echo no-op",
    "lint": "echo no-op",
    "test": "mocha",
    "test:coverage": "c8 npm run test",
    "test:coverage:lcov": "c8 --reporter=lcov npm run test"
  },
  "dependencies": {
    "async": "^2.0.1",
    "lodash": "^4.14.1",
    "moment": "^2.14.1"
  },
  "devDependencies": {
    "c8": "^9.1.0",
    "chai": "^4.4.1",
    "mocha": "^10.3.0"
  }
}
